<!--
 @title Image
 @brief The same source shown plain, rotated + scaled, and recoloured.

 `src` points at a registered image. The second copy sets `rotation` (in
 0.1° steps) with `scale_x`/`scale_y` (256 = 100 %) around a centred pivot.
 The third uses the `image_recolor` style properties to tint the pixels.
-->
<screen>
	<styles>
		<style name="style_tint"
			image_recolor="0x3b82f6" image_recolor_opa="80%" />
	</styles>

	<view flex_flow="column" style_pad_row="14" style_pad_all="16"
		style_flex_cross_place="center">

		<!-- 💡 Same image three ways: original, transformed, recoloured. -->
		<lv_label width="100%" style_text_align="center" text="Image: transform and recolor" />

		<lv_obj width="280" height="120" style_bg_opa="0" style_pad_all="0"
			flex_flow="row" style_flex_main_place="space_evenly"
			style_flex_cross_place="center">

			<lv_image src="img_logo" />

			<lv_image src="img_logo"
				rotation="300" scale_x="180" scale_y="180"
				pivot_x="24" pivot_y="24" />

			<lv_image src="img_logo">
				<style name="style_tint" />
			</lv_image>
		</lv_obj>
	</view>
</screen>
