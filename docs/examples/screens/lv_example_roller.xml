<!--
 @title Roller
 @brief Infinite scrolling, a visible row count, and subject binding.

 `options` takes the list plus a mode parameter (`options-mode="infinite"`)
 so it loops endlessly. `visible_row_count` sets how many rows are shown,
 and `bind_value="subject_index"` keeps the selection in a subject that the
 label below mirrors.
-->
<screen>
	<styles>
		<style name="style_roller"
			bg_color="0x0f172a" bg_opa="100%" text_color="0x94a3b8"
			radius="8" pad_all="6" />
		<!-- The centered (selected) row -->
		<style name="style_roller_sel"
			bg_color="0x3b82f6" text_color="0xffffff" />
	</styles>

	<view flex_flow="column" style_pad_row="14" style_pad_all="16"
		style_flex_cross_place="center">

		<!-- 💡 Spin the roller past the ends — it wraps because the mode is infinite. -->
		<lv_label width="100%" style_text_align="center" text="Roller: infinite scroll and binding" />

		<lv_roller
			options="January\nFebruary\nMarch\nApril\nMay\nJune"
			options-mode="infinite"
			visible_row_count="3"
			bind_value="subject_index">
			<style name="style_roller" />
			<style name="style_roller_sel" selector="selected" />
		</lv_roller>

		<lv_label bind_text="subject_index" bind_text-fmt="Month index: %d" />
	</view>
</screen>
